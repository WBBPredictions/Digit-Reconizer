---
title: "Support_Code"
author: "Travis Barton"
date: "6/16/2018"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r Libraries, data, Functions}
library(beepr)
library(ranger)
library(liquidSVM)
library(FNN)
library(readr)

Cross_val_maker <- function(data, alpha)
{
  if(alpha > 1 || alpha <= 0)
  {
    return("Alpha must be between 0 and 1")
  }
  index <- sample(c(1:nrow(data)), round(nrow(data)*alpha))
  train <- data[-index,]
  test <- data[index,]
  return(list("Train" = as.data.frame(train), "Test" = as.data.frame(test)))
}
percent <- function(in_table)
{
  return(sum(diag(in_table))/sum(in_table))
}

dat <- read_csv("~/Desktop/Projects/Group/Mini_train_temp.csv", col_types = cols(X1 = col_skip()))



```


```{R Model Fits, Predictions}
fit.svm <- mcSVM(label ~ ., Train, predict.prob = T, mc_type = "AvA")
fit.knn <- knn(Train[,-1], Test[,-1], cl = Train[, 1], algorithm = "brute", prob = T)
fit.rf <- ranger(label~ ., Train, num.trees = 500, mtry = 60)

pred.svm <- predict(fit.svm, Test[,-1])
pred.knn <- fit.knn
pred.rf <- predict(fit.rf, Test[,-1])
```

```{r Decision Alg}
Predicted_Survival <- function(pred.svm, pred.knn, pred.rf)
{
  
}


```


